{
  "source_of_truth": "emit_event_calls",
  "required_event_types": [
    "project.changed",
    "application.changed",
    "user_profile.changed",
    "deadline.changed",
    "import.completed",
    "recs.reindex_requested"
  ]
}
